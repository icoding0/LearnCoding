<!--components/bb/bb.wxml-->
<view class="tab-control">
  <block wx:for="{{titles}}" wx:key="{{index}}">
    <view class="tab-item {{cindex == index ? 'active' : '' }}" bindtap="itemclick" data-index="{{index}}"><text>{{item}}</text></view>
  </block>
</view>
